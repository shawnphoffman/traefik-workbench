# Path to the directory containing YAML files to edit
DATA_DIR=./test-data

# Path to the directory containing template YAML files
TEMPLATES_DIR=./test-templates

# Whether templates are read-only (templates cannot be modified via the UI)
TEMPLATES_READONLY=true

# Port the server listens on
PORT=3000
